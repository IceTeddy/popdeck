# PopDeck v0.1.5

## 更新内容

- 设置页底部新增当前版本号显示，便于确认自动更新后的版本。

下载 `PopDeck-0.1.5.dmg`，打开后将 `PopDeck.app` 拖到“应用程序”文件夹。

## What's Changed

- Added the current version number at the bottom of Settings to make update testing easier.

Download `PopDeck-0.1.5.dmg`, open it, and drag `PopDeck.app` to the Applications folder.
